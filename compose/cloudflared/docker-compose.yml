services:
  cloudflared:
    image: cloudflare/cloudflared
    container_name: cloudflared
    environment:
      - TZ=${TZ}
      - TUNNEL_TOKEN=${TOKEN}
    restart: on-failure
    command: tunnel --no-autoupdate run
    network_mode: host
    user: root
